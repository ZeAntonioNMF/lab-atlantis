version: 3
projects:
  - name: infra-test
    dir: terraform/
    workspace: default
    terraform_version: v1.10.5
    autoplan:
      when_modified: ["*.tf", "*.tfvars", "*.hcl"]
      enabled: true
    apply_requirements: [approved]
    workflow: default
